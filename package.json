{
  "name": "finanzasve",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint --ext .js .",
    "format": "prettier --write \"**/*.{js,css,html,json}\"",
    "audit": "npx lighthouse http://localhost:5000 --only-categories=performance,accessibility,best-practices,seos --output html --output-path ./lighthouse-report.html"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0",
    "eslint-config-prettier": "^8.0.0"
  }
}